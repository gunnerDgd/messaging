#pragma once
#include <messaging/defines/handle/opaque.h>

synapse_messaging_opaque_handle_declare
	(synapse_messaging_message);

synapse_messaging_opaque_handle_declare
	(synapse_messaging_route);

synapse_messaging_opaque_handle_declare
	(synapse_messaging_endpoint);